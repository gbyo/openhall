# ADR 0002: PostgreSQL-only persistence and UUIDv7

Status: accepted.

PostgreSQL 18 is the sole production database and database-test target. Kysely preserves SQL
semantics without owning the model. Canonical identifiers are PostgreSQL UUIDs generated by
`uuidv7()`, providing globally safe public identity with useful insertion locality. Migrations are
forward-only and reviewed.
