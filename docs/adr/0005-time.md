# ADR 0005: Time semantics

Status: accepted.

Real events use UTC-capable `timestamptz` and Temporal.Instant. School days and bell times use
PostgreSQL `date`/`time` and Temporal.PlainDate/PlainTime, resolved only with the school's IANA zone.
An injectable Clock makes behavior deterministic. JavaScript Date is excluded from scheduling
logic.
