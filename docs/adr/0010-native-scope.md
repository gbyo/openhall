# ADR 0010: Native applications are outside Phase 1

Status: accepted.

Phase 1 establishes a same-origin web platform and stable API boundaries. Native mobile and watch
clients, mobile authentication, APNs, and FCM are deferred until the core authorization, workflow,
privacy, and operational contracts are proven. Future clients will resolve to the same application
principal and command APIs.
